// 1
const observer1 = new IntersectionObserver(entries =>
{
  entries.forEach(entry =>
  {
    if(entry.isIntersecting)
    {
      entry.target.classList.add('show1');
    }
    // else
    // {
    //   entry.target.classList.remove('show1');
    // }
  });
});

const hidden1_elements = document.querySelectorAll('.hidden1');
hidden1_elements.forEach(el =>
{
  observer1.observe(el);
})


// 2
const observer2 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show2');
      }
      // else
      // {
      //   entry.target.classList.remove('show2');
      // }
    });
  });
  
  const hidden2_elements = document.querySelectorAll('.hidden2');
  hidden2_elements.forEach(el =>
  {
    observer2.observe(el);
  })


// 3
const observer3 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show3');
      }
      // else
      // {
      //   entry.target.classList.remove('show3');
      // }
    });
  });
  
  const hidden3_elements = document.querySelectorAll('.hidden3');
  hidden3_elements.forEach(el =>
  {
    observer3.observe(el);
  })


// 4
const observer4 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show4');
      }
      // else
      // {
      //   entry.target.classList.remove('show4');
      // }
    });
  });
  
const hidden4_elements = document.querySelectorAll('.hidden4');
hidden4_elements.forEach(el =>
{
  observer4.observe(el);
})

// 5
const observer5 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show5');
      }
      // else
      // {
      //   entry.target.classList.remove('show5');
      // }
    });
  });
  
const hidden5_elements = document.querySelectorAll('.hidden5');
hidden5_elements.forEach(el =>
{
  observer5.observe(el);
})

// 6
const observer6 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show6');
      }
      // else
      // {
      //   entry.target.classList.remove('show6');
      // }
    });
  });
  
const hidden6_elements = document.querySelectorAll('.hidden6');
hidden6_elements.forEach(el =>
{
  observer6.observe(el);
})

// 7
const observer7 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show7');
      }
      // else
      // {
      //   entry.target.classList.remove('show7');
      // }
    });
  });
  
const hidden7_elements = document.querySelectorAll('.hidden7');
hidden7_elements.forEach(el =>
{
  observer7.observe(el);
})

// 8
const observer8 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show8');
      }
      // else
      // {
      //   entry.target.classList.remove('show8');
      // }
    });
  });
  
const hidden8_elements = document.querySelectorAll('.hidden8');
hidden8_elements.forEach(el =>
{
  observer8.observe(el);
})

// 9
const observer9 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show9');
      }
      // else
      // {
      //   entry.target.classList.remove('show9');
      // }
    });
  });
  
const hidden9_elements = document.querySelectorAll('.hidden9');
hidden9_elements.forEach(el =>
{
  observer9.observe(el);
})

// 10
const observer10 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show10');
      }
      // else
      // {
      //   entry.target.classList.remove('show10');
      // }
    });
  });
  
const hidden10_elements = document.querySelectorAll('.hidden10');
hidden10_elements.forEach(el =>
{
  observer10.observe(el);
})

// 11
const observer11 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show11');
      }
      // else
      // {
      //   entry.target.classList.remove('show11');
      // }
    });
  });
  
const hidden11_elements = document.querySelectorAll('.hidden11');
hidden11_elements.forEach(el =>
{
  observer11.observe(el);
})

// 12
const observer12 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show12');
      }
      // else
      // {
      //   entry.target.classList.remove('show12');
      // }
    });
  });
  
const hidden12_elements = document.querySelectorAll('.hidden12');
hidden12_elements.forEach(el =>
{
  observer12.observe(el);
})

// 13
const observer13 = new IntersectionObserver(entries =>
  {
    entries.forEach(entry =>
    {
      if(entry.isIntersecting)
      {
        entry.target.classList.add('show13');
      }
      // else
      // {
      //   entry.target.classList.remove('show13');
      // }
    });
  });
  
const hidden13_elements = document.querySelectorAll('.hidden13');
hidden13_elements.forEach(el =>
{
  observer13.observe(el);
})